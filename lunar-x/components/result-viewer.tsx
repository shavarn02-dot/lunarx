'use client'
import {useState} from 'react'
import type {ImageInfo,RegistrationResult} from './types'
type Mode='overlay'|'checkerboard'|'matches'|'difference'|'split'
export function ResultViewer({result,source,reference}:{result:RegistrationResult|null;source:ImageInfo;reference:ImageInfo}){const [mode,setMode]=useState<Mode>('overlay');const [split,setSplit]=useState(50);const file=mode==='overlay'?result?.images?.registered:mode==='checkerboard'?result?.images?.checkerboard:mode==='matches'?result?.images?.matches:mode==='difference'?result?.images?.difference:result?.images?.registered;const src=file?`/images/${file}`:reference.preview;return <section className="result panel" aria-labelledby="result-title"><div className="result-head"><div><p className="section-label">Result & verification</p><h2 id="result-title">Registered image</h2></div><span className={`result-status ${result?.success?'ok':''}`}>{result?result.success?'Registration complete':'Registration failed':'No result yet'}</span></div><div className="view-tabs" role="tablist" aria-label="Image verification views">{([['overlay','Overlay'],['checkerboard','Checkerboard'],['matches','Match vectors'],['difference','Residual heatmap'],['split','Split view']] as [Mode,string][]).map(([id,label])=><button role="tab" aria-selected={mode===id} className={mode===id?'active':''} key={id} onClick={()=>setMode(id)}>{label}</button>)}</div><div className="viewer">{mode==='split'&&result?.images?.registered?<><img src={reference.preview} alt="Reference lunar image"/><div className="split-layer" style={{width:`${split}%`}}><img src={`/images/${result.images.registered}`} alt="Registered source image"/></div><div className="split-line" style={{left:`${split}%`}}/><input aria-label="Adjust split view" type="range" min="0" max="100" value={split} onChange={e=>setSplit(Number(e.target.value))}/><span className="view-label left">Registered</span><span className="view-label right">Reference</span></>:<img src={src} alt={result?`${mode} registration verification`:'Reference image awaiting registration'}/>}</div><p className="viewer-note">{result?'Visualizations generated by the registration pipeline.':'Run registration to replace this reference preview with measured pipeline output.'}</p></section>}
