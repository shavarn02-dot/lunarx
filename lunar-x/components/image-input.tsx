import type {ImageInfo} from './types'

export function ImageInput({label,image,onUpload,uploading}:{label:string;image:ImageInfo;onUpload:(file:File)=>void;uploading:boolean}){return <article className="image-input"><div className="section-label">{label}</div><div className="input-preview"><img src={image.preview} alt={`${label.toLowerCase()} lunar image preview`}/></div><div className="file-row"><code title={image.filename}>{image.filename}</code><label className="upload-button">{uploading?'Uploading…':'Replace image'}<input type="file" accept=".png,.jpg,.jpeg,.tif,.tiff" disabled={uploading} onChange={e=>e.target.files?.[0]&&onUpload(e.target.files[0])}/></label></div><dl className="metadata"><div><dt>Instrument</dt><dd>{image.sensor||'Not available'}</dd></div><div><dt>Resolution</dt><dd>{image.resolution||'Not available'}</dd></div><div><dt>Orbit</dt><dd>{image.orbit||'Not available'}</dd></div><div><dt>Provenance</dt><dd>{image.provenance||'Awaiting validation'}</dd></div></dl></article>}
