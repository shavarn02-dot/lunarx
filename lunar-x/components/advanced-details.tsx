import type {RegistrationResult} from './types'
export function AdvancedDetails({result}:{result:RegistrationResult|null}){const m=result?.metrics;return <details className="advanced panel"><summary>Advanced technical details</summary>{!result?<p className="empty">Technical output will appear after registration.</p>:<div className="detail-grid"><Detail label="Model" value={String(result.configuration?.model_type??result.quality_report?.model_type??'Not available')}/><Detail label="Estimator" value={String(result.configuration?.robust_estimator??'USAC_MAGSAC')}/><Detail label="Condition number" value={format(m?.condition_number)}/><Detail label="Grid occupancy" value={unit(m?.grid_occupancy_pct,'%')}/><Detail label="NCC" value={format(m?.photometric_ncc)}/><Detail label="Photometric RMSE" value={unit(m?.photometric_rmse,' intensity')}/><Detail label="Threshold" value={unit(result.configuration?.reproj_thresh as number|undefined,' px')}/><Detail label="Matrix stability" value={m?.is_stable==null?'Not available':m.is_stable?'Stable':'Unstable'}/><div className="matrix"><span>Transformation matrix</span><pre>{result.transformation_matrix?result.transformation_matrix.map(r=>r.map(v=>v.toFixed(6)).join('   ')).join('\n'):'Not available'}</pre></div><div className="matrix"><span>Source record</span><pre>{JSON.stringify(result.source??{},null,2)}</pre></div><div className="matrix"><span>Reference record</span><pre>{JSON.stringify(result.reference??{},null,2)}</pre></div></div>}</details>}
function Detail({label,value}:{label:string;value:string}){return <div className="detail"><span>{label}</span><code>{value}</code></div>}function format(v:number|null|undefined){return v==null||!Number.isFinite(v)?'Not available':String(v)}function unit(v:number|null|undefined,s:string){return v==null||!Number.isFinite(v)?'Not available':`${v}${s}`}
