import type {Config} from './types'

const matcherNames:Record<string,string>={sift:'SIFT',orb:'ORB',superpoint_lightglue:'SuperPoint + LightGlue',loftr:'LoFTR'}
export function RegistrationControls({config,setConfig,onRun,running}:{config:Config;setConfig:(v:Config)=>void;onRun:()=>void;running:boolean}){const patch=(p:Partial<Config>)=>setConfig({...config,...p});return <section className="controls panel"><div><p className="section-label">Registration controls</p><h2>Configure alignment</h2></div><div className="control-grid"><label>Feature matcher<select value={config.method} onChange={e=>patch({method:e.target.value})}>{Object.entries(matcherNames).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Preprocessing<select value={config.preprocessing} onChange={e=>patch({preprocessing:e.target.value})}><option value="clahe">CLAHE</option><option value="gradient">Gradient</option><option value="raw">Raw</option><option value="phase_congruency">Phase congruency</option></select></label><label>Transformation<select value={config.model_type} onChange={e=>patch({model_type:e.target.value})}><option value="affine">Affine</option><option value="homography">Homography</option><option value="rigid">Rigid</option></select></label></div><details><summary>Advanced options</summary><div className="advanced-controls"><label><input type="checkbox" checked={config.subpixel} onChange={e=>patch({subpixel:e.target.checked})}/> Sub-pixel refinement</label><label><input type="checkbox" checked={config.spatial_filter} onChange={e=>patch({spatial_filter:e.target.checked})}/> 8×8 spatial coverage validation</label><label>Reprojection threshold <span><input type="number" min="0.1" max="20" step="0.1" value={config.reproj_thresh} onChange={e=>patch({reproj_thresh:Number(e.target.value)})}/> px</span></label></div></details><button className="primary run" disabled={running} onClick={onRun}>{running?<><span className="spinner"/>Running registration…</>:'Run registration'}</button></section>}
